name: Build documentation

on:
  push:
    branches:    
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: ammaraskar/sphinx-action@master
      with:
        docs-folder: "docs/"
